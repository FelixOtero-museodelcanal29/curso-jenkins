# curso-jenkins

Repo para el curso de jenkins

comando acceder a usuario root de docker en su core linux  (docker exec -it --user root jenkins /bin/bash)

docker ejeciutar mdo interactivo en  el contenedor (docker exec -it jenkins /bin/bash)

activar ngrok (.\\ngrok.exe http 8080)
